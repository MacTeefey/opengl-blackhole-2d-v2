#include <GLFW/glfw3.h>
#include <iostream>

int main() {
    // TODO: Initialize GLFW library
    // Check if initialization failed and return -1 if so

    // TODO: Create a window (800x600) titled "2D Black Hole Simulator"
    // Store it in a GLFWwindow pointer
    // Check if window creation failed, clean up GLFW, and return -1 if so

    // TODO: Clean up GLFW resources before exiting

    return 0;
}
