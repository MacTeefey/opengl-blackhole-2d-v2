#pragma once

#error "TODO: Create constants.h with all namespace definitions - remove this line when done"

// TODO: Add M_PI definition if needed

// TODO: Create BlackHole namespace with:
// - RS (Schwarzschild radius = 1.0f in geometric units)

// TODO: Create Visual namespace with configuration
// - NUM_STARS, CIRCLE_SEGMENTS
// - WINDOW_WIDTH, WINDOW_HEIGHT
// - VIEW_WIDTH (8.0f), VIEW_HEIGHT (6.0f) in geometric units
// - POINT_SOURCE_X, POINT_SOURCE_Y
// - Start frame indices for ray scenarios (ORBITING_START, POINT_SOURCE_START, PARALLEL_START)

// TODO: Create Simulation namespace with parameters
// - MAX_DISTANCE (30.0f RS)
// - BUDGET_PER_FRAME (0.08f)
// - MIN_STEP (0.01f), MAX_STEP (0.1f) for adaptive stepping
